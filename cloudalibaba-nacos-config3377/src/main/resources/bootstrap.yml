server:
  port: 3377
spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848 # nacos作为服务配置中心地址
        file-extension: yaml # 指定yaml 格式的配置(必须是 yaml不能是yml)