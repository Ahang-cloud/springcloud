server:
  port: 9001
spring:
  application:
    name: nacos-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 # nacos的服务地址
management: # 服务信息暴露
  endpoints:
    web:
      exposure:
        include: '*'